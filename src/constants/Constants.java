package constants;

/**
 * In this file we define some global constants which decides the screen size
 * and is used for drawing objects in the correct place.
 */

public interface Constants {

	/*
	 * Define the size of the window (Spelplanen �r 28x31)
	 */
	int SCREEN_WIDTH = 1200 / 2;
	int SCREEN_HEIGHT = 1200 / 2;
}
